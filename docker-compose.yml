version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    command: gunicorn -w 4 -b 0.0.0.0:8000 app:app
    restart: unless-stopped
